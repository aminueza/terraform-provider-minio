{
  "ignorePatterns": [
    {
      "comment": "Local MinIO consoles from the docker-compose dev stack, not reachable from CI",
      "pattern": "^https?://localhost"
    },
    {
      "comment": "RFC 2606 reserved domains used as placeholders in generated resource docs",
      "pattern": "^https?://([^/]*\\.)?example\\.(com|net|org)"
    },
    {
      "comment": "Placeholder etcd endpoints; the comma-separated example parses as an invalid URL and crashes the checker",
      "pattern": "^https?://etcd"
    },
    {
      "comment": "Changelog compare links reference the release tag before it exists; the tag lands minutes later in the same release flow",
      "pattern": "^https://github\\.com/aminueza/terraform-provider-minio/compare/"
    },
    {
      "comment": "Contact addresses in the code of conduct. The checker resolves mailto: links against DNS, so an unrelated lookup failure fails an unchanged file on an unrelated pull request",
      "pattern": "^mailto:"
    }
  ]
}
