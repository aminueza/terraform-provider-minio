---
page_title: "minio_iam_export Data Source - terraform-provider-minio"
subcategory: ""
description: |-
  Exports the entire IAM configuration (users, groups, policies, service accounts) from a MinIO server. The payload is the raw zip MinIO returns, base64-encoded. Treat as sensitive: it can include access keys.
---

# minio_iam_export (Data Source)

Exports the entire IAM configuration (users, groups, policies, service accounts) from a MinIO server. The payload is the raw zip MinIO returns, base64-encoded. Treat as sensitive: it can include access keys.

## Example Usage

```terraform
data "minio_iam_export" "backup" {}

resource "local_sensitive_file" "iam_backup" {
  filename       = "${path.module}/iam-backup-${data.minio_iam_export.backup.sha256}.zip.b64"
  content        = data.minio_iam_export.backup.iam_data
  file_permission = "0600"
}

output "iam_backup_size" {
  value = data.minio_iam_export.backup.size_bytes
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `iam_data` (String, Sensitive) Base64-encoded zip archive containing the exported IAM data. Pass directly to `minio_iam_import.iam_data` to restore on another cluster.
- `id` (String) The ID of this resource.
- `sha256` (String) SHA-256 of the raw (pre-base64) export, useful as a change-detection signal across plans.
- `size_bytes` (Number) Size of the raw export in bytes.
