---
page_title: "minio_kms_keys Data Source - terraform-provider-minio"
subcategory: "Server"
description: |-
  Lists the keys held by the KMS the MinIO server is configured with. Use it to discover which key IDs exist before referring to one in minio_s3_bucket_server_side_encryption, or to assert that a key a configuration depends on is present.
---

# minio_kms_keys (Data Source)

Lists the keys held by the KMS the MinIO server is configured with. Use it to discover which key IDs exist before referring to one in `minio_s3_bucket_server_side_encryption`, or to assert that a key a configuration depends on is present.

Community MinIO serves key listing whenever a KMS is configured. It is the only
KMS key operation besides create and status that the community server exposes,
so this data source works there as well as on AIStor.

## Example Usage

```terraform
data "minio_kms_keys" "all" {}

data "minio_kms_keys" "terraform_managed" {
  pattern = "terraform-*"
}

output "key_names" {
  value = [for key in data.minio_kms_keys.all.keys : key.name]
}

output "terraform_managed_count" {
  value = length(data.minio_kms_keys.terraform_managed.keys)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `pattern` (String) Glob pattern a key name must match to be listed. Defaults to `*`, which lists every key the caller may see.

### Read-Only

- `id` (String) The ID of this resource.
- `keys` (List of Object) Keys the KMS reports, in the order the server returns them. (see [below for nested schema](#nestedatt--keys))

<a id="nestedatt--keys"></a>
### Nested Schema for `keys`

Read-Only:

- `created_at` (String)
- `created_by` (String)
- `name` (String)
