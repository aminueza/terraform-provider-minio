---
page_title: "Data Source minio_server_info - terraform-provider-minio"
subcategory: ""
description: |-
  Reads MinIO server information including version, deployment ID, and storage metrics.
---

# Data Source: minio_server_info

Reads MinIO server information including version, deployment ID, and storage metrics.

This data source reads information about the MinIO server including version, deployment ID, region, and detailed metrics about each server and drive in the cluster.

## Example Usage

```terraform
data "minio_server_info" "current" {}

output "minio_version" {
  value = data.minio_server_info.current.version
}

output "minio_region" {
  value = data.minio_server_info.current.region
}

output "deployment_id" {
  value = data.minio_server_info.current.deployment_id
}

output "storage_summary" {
  value = {
    total_servers = length(data.minio_server_info.current.servers)
  }
}

output "servers" {
  value = data.minio_server_info.current.servers
}

output "all_servers_online" {
  value = alltrue([
    for server in data.minio_server_info.current.servers :
    server.state == "online"
  ])
}
```

## Use Cases

- **Version Checking**: Verify MinIO server version for compatibility checks
- **Capacity Planning**: Monitor total and used storage across the cluster
- **Health Monitoring**: Check server and drive states
- **Deployment Validation**: Verify deployment ID and configuration

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `commit` (String) Git commit hash of the server build
- `deployment_id` (String) Deployment ID of the MinIO cluster
- `id` (String) The ID of this resource.
- `region` (String) Server region
- `servers` (List of Object) List of servers in the cluster (see [below for nested schema](#nestedatt--servers))
- `sqsarn` (List of String) List of configured SQS ARNs for event notifications
- `version` (String) MinIO server version

<a id="nestedatt--servers"></a>
### Nested Schema for `servers`

Read-Only:

- `commit_id` (String)
- `drives` (List of Object) (see [below for nested schema](#nestedobjatt--servers--drives))
- `endpoint` (String)
- `network` (Map of String)
- `state` (String)
- `uptime` (Number)
- `version` (String)

<a id="nestedobjatt--servers--drives"></a>
### Nested Schema for `servers.drives`

Read-Only:

- `available_space` (Number)
- `endpoint` (String)
- `read_throughput` (Number)
- `state` (String)
- `total_space` (Number)
- `used_space` (Number)
- `uuid` (String)
- `write_throughput` (Number)
