---
page_title: "minio_s3_bucket_policy Resource - minio"
subcategory: ""
description: |-
  
---

# minio_s3_bucket_policy (Resource)



## Example Usage

```terraform
# Attach a bucket policy
resource "minio_s3_bucket_policy" "example" {
  bucket = "my-bucket"

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect = "Allow"
        Principal = {
          AWS = ["*"]
        }
        Action = [
          "s3:GetObject"
        ]
        Resource = [
          "arn:aws:s3:::my-bucket/*"
        ]
      }
    ]
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `bucket` (String) Name of the bucket
- `policy` (String) Policy JSON string

### Optional

- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `read` (String)
- `update` (String)

## Import

Import is supported using the following syntax:

```shell
# S3 bucket policies can be imported using the bucket name
terraform import minio_s3_bucket_policy.example bucket-name
```
