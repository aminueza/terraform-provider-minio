---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

{{ tffile "examples/resources/minio_iam_service_account/resource.tf" }}

## Write-Only Secret

Use `secret_key_wo` with `secret_key_wo_version` to rotate service account secrets without storing the submitted value in Terraform state.

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

```shell
# IAM service accounts can be imported using the access key ID
terraform import minio_iam_service_account.example AKIAIOSFODNN7EXAMPLE
```
